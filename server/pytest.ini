[tool:pytest]
# Simple pytest configuration for LawTime undergraduate project
testpaths = tests
python_files = test_*.py
addopts = -v --color=yes --disable-warnings
filterwarnings = ignore::DeprecationWarning